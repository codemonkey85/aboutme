@((MarkupString)html)
@if (AllowPageDownload)
{
    <a download="@ContentFileName"
       href="@MarkdownContentFileName"
       target="_blank"
       class="btn btn-primary">
        @DownloadPageButtonText
    </a>
}
